diff --git a/orchestrate/agent_runner.py b/orchestrate/agent_runner.py
index 3103584..a1894ac 100644
--- a/orchestrate/agent_runner.py
+++ b/orchestrate/agent_runner.py
@@ -24,6 +24,7 @@ from agents import (
     ImpactSimulationAgent,
     ParameterAdjustmentAgent,
     ReroutingAgent,
+    SeverityTriageAgent,
     SubstitutionAgent,
     VisionSpecAgent,
 )
@@ -48,6 +49,7 @@ class AgentRunner:
         self._dt = digital_twin
         self._agents = {
             "vision_spec": VisionSpecAgent(knowledge_graph=self._kg),
+            "severity_triage": SeverityTriageAgent(),
             "cad_spec": CADSpecAgent(knowledge_graph=self._kg),
             "causal_isolation": CausalIsolationAgent(
                 causal_graph=self._cg, knowledge_graph=self._kg
diff --git a/orchestrate/orchestrator.py b/orchestrate/orchestrator.py
index 3e99a63..43c48d5 100644
--- a/orchestrate/orchestrator.py
+++ b/orchestrate/orchestrator.py
@@ -338,6 +338,11 @@ class DecisionOrchestrator:
                     context,
                     StageInput(stage_name="Perception", payload={"vision_data": vision_data, "part_number": part_number}),
                 )
+                await self._runner.run_stage(
+                    "severity_triage",
+                    context,
+                    StageInput(stage_name="Perception", payload={"deviation_mm": out1.result["deviation_mm"]}),
+                )
                 out2, _ = await self._runner.run_stage(
                     "cad_spec",
                     context,
