# Not an MCP server or an OpenAPI spec

Just a plain repo with nothing detect_track() should recognize — used to
prove the "couldn't classify" path in orchestrate/onboarding/inspector.py
degrades cleanly instead of guessing.
